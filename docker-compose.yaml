version: '3.1'

services:
  frontend:
    image: frontend
    build: 
      context: ./my-blog
    ports:
      - "80:80"
    networks: 
      - my_network

  backend:
    image: backend
    build:
      context: ./MyBlog
    ports:
      - "90:90"
    networks: 
      - my_network

networks: 
  my_network: